{
  "name": "ole",
  "version": "0.3.0",
  "description": "Library to integrate OpenLayers 3 with Esri ArcGIS Server REST API",
  "main": "dist/ole.js",
  "scripts": {
    "start": "watchify src/index.js --standalone ole -o examples/ole-debug.js & st -nc",
    "build": "browserify -p browserify-header -p browserify-derequire -t babelify -p [minifyify --no-map] src/index.js --standalone ole -o dist/ole.js && browserify -p browserify-header -p browserify-derequire src/index.js --standalone ole -o dist/ole-debug.js",
    "lint": "eslint src",
    "test": "phantomjs --ssl-protocol=any --ignore-ssl-errors=true ./node_modules/mocha-phantomjs/lib/mocha-phantomjs.coffee test/index.html"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/boundlessgeo/ole.git"
  },
  "keywords": [
    "openlayers",
    "esri",
    "arcgis",
    "rest"
  ],
  "author": "Boundless",
  "license": "Apache",
  "bugs": {
    "url": "https://github.com/boundlessgeo/ole/issues"
  },
  "homepage": "https://github.com/boundlessgeo/ole",
  "dependencies": {
    "babelify": "^6.3.0"
  },
  "devDependencies": {
    "browserify": "^11.1.0",
    "browserify-derequire": "^0.9.4",
    "browserify-header": "^0.9.2",
    "eslint": "^0.22.1",
    "expect.js": "0.3.1",
    "fs-extra": "0.12.0",
    "minifyify": "^7.0.6",
    "mocha": "1.21.5",
    "mocha-phantomjs": "3.5.1",
    "openlayers": "^3.7.0",
    "phantomjs": "1.9.10",
    "st": "^0.5.3",
    "watchify": "^3.2.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
